import React from 'react';
import CartComponent from '../components/Cart';

const Cart = ({ serializedCategory }) => {
  return (
    <React.Fragment>
      <CartComponent />
    </React.Fragment>
  );
};

export default Cart;
